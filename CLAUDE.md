# キミテラス LP — Claude Code 運用規律

サービス紹介ランディングページ（本番 **www.school-signage.net** ／ Next.js ／ Vercel ／ GitHub `cometa-kaito/edix-lp`）。

> 🔗 **このアプリは「キミテラス事業」4 リポジトリの 1 つ＝サービス紹介 LP（プロダクト青橙＋LP 専用アクセント）。**
> 横断の地図は [../CLAUDE.md](../CLAUDE.md)、ブランド/配色の正本は
> [../デザインシステム正本-キミテラス.md](../デザインシステム正本-キミテラス.md)（skill `design-ui`）。
> LP は他面と**独立リポジトリ**なので、配信機能や SoR には関与しない（純粋な公開 LP）。

## ブランド / デザイン（面 = LP）
- **プロダクト（青＋オレンジ）＋ LP 専用アクセント**。値の権威は `app/globals.css`（純 CSS 変数・Tailwind config 無し）。
- 横断規律（グラデ禁止＝ソリッドのみ／色だけで意味を伝えない／`prefers-reduced-motion` ガード）は [../デザインシステム正本-キミテラス.md](../デザインシステム正本-キミテラス.md) を正本とする。生 hex を本書に再掲しない。
- ⚠ **既知の AA 課題**: `.btn-accent` の白文字 on `#F08A45` = 2.48:1（AA 未達・非ブロッキング）。**新規で踏襲しない**。
- 打ち出し方針: 公開面に「特別価格」「実証実験/PoC」を出さない（料金は「お問い合わせ」）。導入実績は中立表現。

## デプロイ / 運用
- Vercel（`main` push で自動デプロイ、または `vercel --prod`）。
- ⚠ **編集は必ず `origin/main` 基点で**。古い feature ブランチを掴むと本番に出ない（過去の事故）。
- Next.js のバージョン差異に注意（`node_modules/next/dist/docs/` を確認してから書く）。

## 横断で触る前に
LP 単体で完結する作業がほとんど。ブランド値を触るなら [../デザインシステム正本-キミテラス.md](../デザインシステム正本-キミテラス.md) を先に読む（4 面のブランド取り違え防止）。
